import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("SwiftKeychainKit TestHost")
                .font(.headline)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
